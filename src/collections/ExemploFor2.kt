package collections

fun main() {
    val alunos = arrayListOf<String>("Amanda", "André", "Bernardo", "Carlos")

    alunos.forEach { println(it) } // Serve para navegar entre os elementos da lista
}